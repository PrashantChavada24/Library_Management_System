# 📚 Library Management System (Java CLI)

A simple **Command-Line Interface (CLI)** based Library Management System developed in **Java**.  
This project allows users to **add, remove, search, and view books** using file handling for data storage.

---

## 🚀 Features

- Add new books to the library
- Remove books using Book ID
- Search for books by ID
- View all available books
- Persistent storage using file handling (`books.txt`)
- Input validation and error handling
- Menu-driven interactive CLI

---

## 🛠️ Technologies Used

- Java (JDK 8+)
- File Handling (`FileReader`, `FileWriter`, `BufferedReader`)
- Command Line Interface (CLI)




